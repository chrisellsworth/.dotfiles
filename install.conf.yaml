---
- defaults:
    link:
      relink: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.Brewfile:
    ~/.gitconfig:
    ~/.vimrc:
    ~/.zshrc:

- brewfile:
    - Brewfile
