---
- defaults:
    link:
      relink: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.Brewfile:
    ~/.gitconfig:
    ~/.p10k.zsh:
    ~/.vimrc:
    ~/.zshrc:

- brewfile:
    - Brewfile
